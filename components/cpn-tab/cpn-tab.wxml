<view class="tabContainer">
<view wx:for="{{tabItems}}" class="tabItemContainer" wx:key="id">
<view class="tabItem {{activeIndex==index?'active':''}}" bind:tap="selectTabItem" data-index="{{index}}">
{{item.name}}
</view>
</view>
</view>
